# Feature Spec: 排行榜畫面

> Auto-generated by Git Worktree Design Skill.

## Branch Info

| Field         | Value                              |
|---------------|------------------------------------|
| Branch Name   | `feature/leaderboard`              |
| Based On      | `polish/ui-sizing-layout-grade-dialogue` |
| Worktree Path | `/Users/clemmy.liao/vibe-demo-leaderboard` |
| Created At    | 2026-03-03                         |

## Goal

新增 LEADERBOARD 畫面，玩家可於結果頁點入查看全服排名（前 20 名），支援按角色篩選。

## Implementation Scope

- [ ] `src/hooks/useGameState.js` — `initialState` 加 screen enum `"LEADERBOARD"`，新增 actions：`GO_TO_LEADERBOARD`、`BACK_FROM_LEADERBOARD`
- [ ] `src/App.jsx` — `screens` map 加 `LEADERBOARD: <LeaderboardScreen ...>`
- [ ] `src/screens/LeaderboardScreen.jsx` — 新建，顯示排名列表（名次、玩家名、角色icon、分數、好感度）、角色篩選 tabs、loading/error 狀態
- [ ] `src/services/gasApi.js` — 新增 `fetchLeaderboard(characterId)` → `GET ${GAS_URL}?action=leaderboard&character=<id>`
- [ ] `gas/Code.gs` — `doGet` handler 讀取 `回答` sheet，依 `score` 降序回傳前 20 筆，格式 `{ rows: [...] }`
- [ ] `src/screens/ResultsScreen.jsx` — 底部加「查看排行榜 →」按鈕，dispatch `GO_TO_LEADERBOARD`

## Acceptance Criteria

- 結果頁有「查看排行榜」按鈕
- 排行榜顯示前 20 名，含名次、玩家名稱、角色、答對題數、好感度
- 可點選角色 tab 篩選（zero / phantom / cipher / glitch / 全部）
- GAS 回應中顯示 loading spinner，失敗顯示錯誤提示
- 「← 返回結果」按鈕可回到 RESULTS

## Technical Constraints

- 所有 source files 為 `.jsx` / `.js`，不使用 TypeScript
- 樣式沿用現有 Tailwind design tokens（`dark-card`、`neon-cyan` 等）
- 不引入新的 npm 套件
- GAS 端使用 `text/plain` Content-Type 避免 CORS preflight
- Demo mode（GAS_URL 為空）回傳假排名資料

## Cross-Branch Notes

- 同樣修改 `useGameState.js` 與 `App.jsx` 的分支：`feature/character-gallery`
- 推薦 merge 順序：`sound-effects` → **`leaderboard`（本支）** → `character-gallery`
- 合併時注意 `useGameState.js` 的 screen enum 與 `App.jsx` 的 screens map 需手動合併
