# Feature Spec: 攻略評分儀表板

> This file was auto-generated by the Git Worktree Design Skill as a development guide for AI agents.

## Branch Info

| Field         | Value                                          |
| ------------- | ---------------------------------------------- |
| Branch Name   | `feature/score-dashboard`                      |
| Based On      | `main`                                         |
| Worktree Path | `/Users/clemmy.liao/vibe-demo-dashboard`       |
| Created At    | 2026-03-06                                     |

## Goal

新增攻略評分儀表板頁面，讓玩家可以回顧所有角色的歷史攻略成績、解鎖狀態與最佳紀錄，增加遊戲的收集與重玩動力。

## Implementation Scope

- [ ] 新增 `DASHBOARD` screen state，擴充 `src/hooks/useGameState.js` 的 screen enum
- [ ] 在 `src/App.jsx` 的 `screens` map 加入 `DASHBOARD` 對應的 component
- [ ] 建立 `src/screens/DashboardScreen.jsx`（或 `src/components/results/DashboardScreen.jsx`）
- [ ] 從 `src/hooks/useHistory.js` 讀取 localStorage 歷史資料，展示各角色最佳成績
- [ ] 各角色卡片顯示：最高 affection 分數、最佳 grade（S/A/B/C/D）、是否解鎖告白、最後遊玩時間
- [ ] 總覽統計區塊：已攻略角色數、平均 affection、最高分角色
- [ ] Grade 進度條視覺化（純 Tailwind CSS，不引入圖表套件）
- [ ] 在 `src/screens/HomeScreen.jsx` 新增「攻略紀錄」按鈕，dispatch 進入 DASHBOARD
- [ ] 從 DASHBOARD 可 dispatch 回 HOME screen

## Acceptance Criteria

- Dashboard 正確顯示所有已玩角色的歷史最佳 affection 與 grade
- 未玩過的角色顯示「未攻略」灰色狀態卡片
- 已解鎖告白的角色卡片有特殊標記（neon glow 或 💖 icon）
- 總覽統計數字正確計算（已攻略數、平均分、最高分角色）
- 從 HomeScreen 點擊「攻略紀錄」可進入 Dashboard
- 從 Dashboard 點擊返回可回到 HomeScreen
- 使用 Framer Motion `AnimatePresence` 做 screen 進出動畫（與其他 screen 一致）

## Technical Constraints

- `src/hooks/useHistory.js` 現有介面不變，只新增讀取用的 helper function（如 `getAllHistory()`）
- 沿用 `dark-card`, `dark-panel`, `neon-*` 設計系統 token
- 不引入圖表套件，grade 視覺化用 Tailwind `w-*` 動態寬度實作進度條
- 角色資料從 `src/constants/characters.js` 讀取（id, name, neon color token）
- ScreenWrapper 包裝確保動畫一致性

## Cross-Branch Notes

- 不依賴 `feature/quiz-optimization`，可獨立開發
- 建議在 `feature/quiz-optimization` 合併後再合併此分支，以減少 `App.jsx` 和 `useGameState.js` 的合併衝突
- 若兩分支都修改了 `useGameState.js`，合併時需手動整合 state 欄位
